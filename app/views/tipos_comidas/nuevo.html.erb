<h1>Formulario para crear un nuevo tipo de comida</h1>

<%= form_with(model: @nuevo_tipo_comida, url: nuevo_tipo_comida_path) do |formulario| %>
    <%= formulario.label        :tipo %>
    <%= formulario.text_field   :tipo %>
    <%= formulario.submit 'Crear' %>
<% end %>